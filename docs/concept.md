# DOSBox-X Launcher — 项目构想

## 项目概述

一个基于 Python 3.11 + PyQt5 的跨平台桌面应用，作为 DOSBox-X 的图形化前端。
目标是让用户方便地管理 DOS 游戏库，快速配置并启动游戏，无需手动编写 `.conf` 文件。

**技术栈**
- 语言：Python 3.11+
- UI：PyQt5 Widgets
- 数据存储：JSON
- 后端：DOSBox-X（外部进程，用户自行安装）
- 平台：Windows / Linux / macOS

---

## 界面布局

窗口从上到下分为四个区域：菜单栏、工具栏、分类 Tab、游戏列表。

```
┌──────────────────────────────────────────────────────────────────┐
│ 文件  编辑  帮助                                                   │  ← 菜单栏
├──────────────────────────────────────────────────────────────────┤
│ [+添加]  [编辑]  [删除]  [启动]  |  [搜索栏...]  |  [排序▼] [设置] │  ← 工具栏
├──────────────────────────────────────────────────────────────────┤
│ 全部(42) │ 角色扮演(8) │ 即时战略(5) │ 益智(12) │ 动作(6) │ [+] │  ← 分类 Tab
├──────────────────────────────────────────────────────────────────┤
│ 封面  游戏名称              分类      最近游玩          游玩次数    │
│ ────────────────────────────────────────────────────────────── │
│ [图] 仙剑奇侠传             角色扮演  2026-03-01        12 次      │
│ [图] 轩辕剑外传：枫之舞     角色扮演  2026-02-14         3 次      │  ← 游戏列表
│ [图] 红色警戒               即时战略  2026-01-20        28 次      │
│ [图] 命令与征服             即时战略  —                  0 次      │
│  ...                                                             │
├──────────────────────────────────────────────────────────────────┤
│ 共 42 个游戏  |  当前分类：全部  |  最近游玩：仙剑奇侠传            │  ← 状态栏
└──────────────────────────────────────────────────────────────────┘
```

---

## 功能模块概要

1. **分类管理** — 用户自定义游戏分类（增删改、拖拽排序），始终保留一个不可删除的"全部"分类
2. **游戏库管理** — 手动添加或批量扫描导入游戏；编辑游戏信息；删除仅移除条目不删文件
3. **游戏浏览** — 列表视图展示封面、名称、分类、游玩信息；支持搜索过滤和多维度排序
4. **封面图管理** — 支持用户指定本地图片、从截图库选择、或使用默认占位图
5. **截图管理** — 关联 DOSBox-X 截图输出目录，按游戏归属截图，可设为封面
6. **DOSBox-X 配置** — 全局默认配置 + 每游戏差异覆盖的继承机制；内置预设模板
7. **一键启动** — 合并配置、生成临时 conf、调用 subprocess 启动 DOSBox-X、记录游玩数据
8. **全局设置** — DOSBox-X 路径、截图目录、游戏库目录、语言、主题等
9. **导入 / 导出** — 游戏库打包导出/导入（ZIP），支持单游戏配置导出
10. **其他** — 系统托盘、首次运行向导、游玩记录统计、游戏备注

---

## 开发阶段规划

| 阶段 | 功能 | 目标 |
|------|------|------|
| **MVP** | 游戏库 CRUD + 列表视图 + 配置对话框 + 一键启动 + 全局设置 | 能用 |
| **v1.1** | 批量扫描导入 + 截图管理 + 封面图选择 + 配置模板 | 好用 |
| **v1.2** | 导入导出 + 搜索/排序 + 系统托盘 + 首次向导 | 完整 |
| **v2.0** | 游玩时长统计 + 游戏备注 + 多语言支持 | 精致 |
